{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "A simple HTML quiz application with TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npx --yes serve . -p 3000"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
